import type React from "react";

import styles from "./HomepageConnected.module.scss";

export const HomepageConnected = (): React.JSX.Element => {
  return (
    <div className={styles.homepage_connected_container}>HomepageConnected</div>
  );
};
