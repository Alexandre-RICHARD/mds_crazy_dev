import type React from "react";

import styles from "./AccountAccess.module.scss";

export const AccountAccess = (): React.JSX.Element => {
  return (
    <div className={styles.account_access_container}>
      <div>AccountAccess</div>
    </div>
  );
};
